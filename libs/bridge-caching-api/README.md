# Bridge Caching API

## Description
This is where all the requests to the [caching API](https://github.com/hcabel/web-monorepo/tree/DEV/apps/caching-api) are created with the right routes and interfaces.
So you dont have to worry about the API, just call the function with the right parameters, and it will do the rest.